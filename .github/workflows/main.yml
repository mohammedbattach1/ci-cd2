name: CI SaaS BTP

on:
  push:
    branches:
      - main

jobs:
  test-action:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout du code
        uses: actions/checkout@v4

      - name: Test simple
        run: echo "GitHub Actions خدام مزيان فهاد البروجي!"
